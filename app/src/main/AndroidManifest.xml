<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AcademiaGo">

        <!-- Auth -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AcademiaGo">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.auth.RegisterActivity" />
        <activity android:name=".ui.auth.ChangePasswordActivity" />

        <!-- Student -->
        <activity android:name=".ui.student.StudentDashboardActivity" />
        <activity android:name=".ui.student.AssignmentsActivity" />
        <activity android:name=".ui.student.SubmitAssignmentActivity" />
        <activity android:name=".ui.student.GradesActivity" />
        <activity android:name=".ui.student.MessagesActivity" />
        <activity android:name=".ui.student.NoticesActivity" />

        <!-- Teacher -->
        <activity android:name=".ui.teacher.TeacherDashboardActivity" />
        <activity android:name=".ui.teacher.UploadAssignmentActivity" />
        <activity android:name=".ui.teacher.PublishNoticeActivity" />
        <activity android:name=".ui.teacher.ManageCoursesActivity" />
        <activity android:name=".ui.teacher.ManageStudentsActivity" />

        <!-- Admin -->
        <activity android:name=".ui.admin.AdminDashboardActivity" />
        <activity android:name=".ui.admin.AdminCreateUserActivity" />

    </application>
</manifest>
